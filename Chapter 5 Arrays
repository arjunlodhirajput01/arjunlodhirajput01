Solution 1:
// Write a program to find the average marjs of class. Suppose that there are n students in the class.

#include<stdio.h>
int main()
{
    int sum,marks[25],n,i;
    float average;
    printf("Enter the number of students : ");
    scanf("%d", &n);
    printf("Enter the marks of students : ");
    sum = 0;
    for(i=1; i<=n; i++)
    {
        scanf("%d", &marks[i]);
        sum += marks[i];
    }
    average=sum/n;
    printf("The average marks of class is %f", average);
}

Output: /tmp/KeRnQ5Fmam.o
Enter the number of students : 5
Enter the marks of students : 10
20
30
40
50
The average marks of class is 30.000000

Solution 2:
// Write a program to find the largest number from given n numbers.

#include<stdio.h>
int main()
{
    int n,i;
    float a[20],big;
    printf("Enter the number : ");
    scanf("%d", &n);
    printf("Enter %d real numbers \n");
    for(i=1; i<=n; i++)
       scanf("%f", &a[i]);
    big = a[i];
    for(i=2; i<=n; i++)
       if(a[i]>big) big = a[i];
    printf("The big number is : %f", big);
}

Output: Enter the number 5
Enter 5 real numbers 
12
45.5
-28
-98
55.5
The big number is : 55.500000

Solution 3: 

// Program to convert Decimal number to its equivalent Binary number.

#include<stdio.h>
int main()
{
    int dno,r[20],i=1,j;
    printf("Enter the Decimal Number : ");
    scanf("%d", &dno);
    do
    {
        r[i] =dno%2;
        dno = dno/2;
        i++;
    } while(dno>0);
    printf("Equivalent Binary Number is : ");
        for(j=i-1;j<=1;j--);
        printf("%d", r[j]);
}

Output : /tmp/KeRnQ5Fmam.o
Enter the Decimal Number : 9
Equivalent Binary Number is : 1

Solution 4:


